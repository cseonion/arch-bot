# 기여 절차

작업 전 루트 `AGENTS.md`와 `for_codex/README.md`에서 현재 유효한 저장소 규칙을
확인합니다.

`main`에 직접 작업하기보다 기능별 브랜치와 PR을 사용합니다.

1. `codex/<작업명>` 또는 `feature/<작업명>` 브랜치를 만듭니다.
2. 한 커밋에는 하나의 논리적 변경을 담습니다.
3. `ruff check .`와 `pytest`를 실행합니다.
4. 사용자 기능 변경은 `README.md`, 개발 변경은 `README_dev.md`에 반영합니다.
5. `CHANGELOG.md`의 `[미출시]` 사용자용·개발자용 두 단을 갱신합니다.
6. PR 템플릿에 변경 이유와 검증 결과를 기록합니다.
7. 토큰, API 키, 고객·프로젝트 정보는 커밋하지 않습니다.

권장 커밋 접두사는 `feat:`, `fix:`, `docs:`, `test:`, `chore:`입니다.
