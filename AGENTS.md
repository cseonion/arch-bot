# arch-bot 저장소 작업 규칙

이 저장소에서 작업하기 전에 `for_codex/README.md`와 변경 범위에 해당하는 규칙
파일을 읽는다.

- 사용자의 새 지시가 이후 작업에도 지속되는 기준이면 같은 변경에서
  `for_codex` 정본을 갱신한다.
- 새 지시가 기존 기준을 대체하면 상충하는 예전 문장을 함께 수정하거나 제거한다.
- 사용자 문서, PR 설명, CHANGELOG와 GitHub Release는 한국어로 작성한다.
- 기능 변경마다 `CHANGELOG.md`의 `[미출시]` 두 단을 함께 갱신한다.
- 채널 기능 변경 시 코드, 에이전트 프로필, 테스트, 사용자/개발자 문서와
  `for_codex/channel-agent-rules.md`를 함께 점검한다.
- 실제 구현하고 검증한 범위만 지원된다고 표현한다. 선언만 된 Tool/MCP를 실행
  가능하다고 설명하지 않는다.
- `.env`, 토큰, 고객·프로젝트 원본 자료를 커밋하지 않는다.
- 커밋 전에 `ruff check .`와 `pytest`를 실행한다.
